{
  "name": "the-app",
  "engines": {
    "node": ">=8.0.0"
  },
  "private": true,
  "version": "2.0.1",
  "license": "MIT",
  "dependencies": {
    "cds-size-plugin": "^1.1.3",
    "rando-names": "^0.0.0-semantically-released",
    "react": "^16.7.0",
    "react-app-rewired": "2.0.1",
    "react-dom": "^16.7.0",
    "react-scripts": "2.1.3"
  },
  "scripts": {
    "dev": "react-app-rewired start",
    "build_it": "GENERATE_SOURCEMAP=false react-app-rewired build",
    "test": "react-app-rewired test --env=jsdom",
    "eject": "react-scripts eject",
    "start": "npm run build_it && node index.js"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {}
}
